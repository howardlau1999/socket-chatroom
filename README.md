# Socket Chatroom

Me learning socket API through practice :-p

# Build & Run

```bash
make server client
```

Start server by:

```bash
./bin/server
```

Start a client by:

```bash
./bin/client server_host nickname
```
